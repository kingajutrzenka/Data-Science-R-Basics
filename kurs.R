
#ax^2 + bx + c = 0
#x^2 + x - 1 = 0

a <- 1
b <- 1
c <- -1

#it's the same

print(a)
print(b)
print(c)

a
b
c

#show every objects

ls()

#delta

delta <- sqrt(b^2 - 4*a*c)
x1 <- ((-b + delta))/(2*a)
x2 <- ((-b - delta))/(2*a)
x1
x2
